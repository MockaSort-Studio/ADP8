{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "gen-compile-commands",
            "type": "shell",
            "command": "bazel run //:compile_cc && bazel run //:compile_cc_fix",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}