%hall-local v1 | MockaSort-Studio/ADP8 | 2026-03-16
!arch .github/workflows/checks.yml: PR comment-triggered build-and-test (3-job: trigger/build/finish); uses MockaSort-Studio/actions
!arch .github/workflows/build_status.yml: push-to-main build; uses MockaSort-Studio/actions build-bazel with config=main
!arch .github/actions/setup_environment/action.yml: installs llvm@18 via brew; sets up Bazel caches
!arch core/: Javelina-RT — FastDDS-based real-time framework (C++)
!arch applications/: AV tasks (planning, control, egomotion, teleop)
!arch messages/: DDS message definitions
!con Bazel is the only build system — never suggest CMake
!con No preemptive synchronization on parameters — no mutexes without real concurrent use case
!con Compile-time preferred over runtime (templates, FNV-1a, tag dispatch)
!con C++ Google Style Guide + .clang-format enforced; Python: ruff + ty
!con Do not touch applications/ ROS2 purge — associate is handling it
!con .github folder only — do not change other actions or workflows
!dec 2026-03-16 #77 old-major: replaced bazel-diff based CI with MockaSort reusable actions; removed build_and_test/ local action and build_and_test.sh
!log 2026-03-16 #77 old-major → pr_created: refactored checks.yml + build_status.yml to use MockaSort-Studio/actions; PR #78 merged
!log 2026-03-16 #77 old-major → comment_posted: CI re-dispatch after expected failure of pre-merge old workflow run; issue closed, work complete
